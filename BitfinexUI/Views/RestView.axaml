<UserControl Background="#e8faf6" xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:vm="using:BitfinexUI.ViewModels"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
			 xmlns:local="clr-namespace:BitfinexUI.ViewModels"
			 x:DataType="local:RestViewModel"
             x:Class="BitfinexUI.Views.RestView">

	<Grid RowDefinitions="Auto,*" Margin="10">

		<!-- Выбор валютной пары -->

		<Grid Grid.Row="0" ColumnDefinitions="200, *">
			<TextBlock Text="Выберите валютную пару:" Grid.Column="0" VerticalAlignment="Center" Margin="0 0 10 0"/>
			<ComboBox x:Name="CurrencyPairsComboBox" Grid.Column="1" HorizontalAlignment="Stretch">
				<ComboBoxItem>BTCUSD</ComboBoxItem>
				<ComboBoxItem>XRPUSD</ComboBoxItem>
				<ComboBoxItem>XMRUSD</ComboBoxItem>
				<ComboBoxItem>DASHUSD</ComboBoxItem>
			</ComboBox>
		</Grid>

		<Grid Grid.Row="1">
			<TabControl Margin="0 20 0 10" ItemsSource="{Binding Tabs}">
				<!-- Шаблон для заголовка вкладки -->
				<TabControl.ItemTemplate>
					<DataTemplate DataType="vm:TradesViewModel">
						<TextBlock Text="{Binding Header}" />
					</DataTemplate>
				</TabControl.ItemTemplate>
			</TabControl>
		</Grid>
	</Grid>

</UserControl>
